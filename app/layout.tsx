import type { Metadata } from "next";
import { Inter, Playfair_Display } from "next/font/google";
import "./globals.css";
import Navbar from "@/components/Navbar";
import Footer from "@/components/Footer";

const inter = Inter({ subsets: ["latin"], variable: '--font-inter' });
const playfair = Playfair_Display({ subsets: ["latin"], variable: '--font-playfair' });

export const metadata: Metadata = {
  title: "Himalayan Pizza Palace - Authentic Italian Cuisine in Kathmandu",
  description: "Experience the finest Italian-Nepali fusion cuisine in the heart of Kathmandu. Premium pizzas, pastas, and authentic Italian dishes with a Himalayan touch.",
  keywords: "Italian restaurant, pizza Kathmandu, Italian food Nepal, fine dining Kathmandu, pizza delivery",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={`${inter.variable} ${playfair.variable} font-sans antialiased bg-royal-black text-royal-cream`}>
        <Navbar />
        <main className="min-h-screen">
          {children}
        </main>
        <Footer />
      </body>
    </html>
  );
}
